For the database:

    -> Table that will hold the product id (primary key), product image, product description, product url 
    -> Table that will hold review id (primary key), product id, chart 1 associated with review, algorithm 1 result, chart 2 associated with review, algorithm 2 result. 
        -> Multiple reviews can relate to one product id. 